apiVersion: v1
kind: Service
metadata:
  name: yourdisc-service
  labels:
    app: yourdisc-api
spec:
  type: ClusterIP
  selector:
    app: yourdisc-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8000
